/**
 * implement binary search
 * 
 * Search a sorted array by dividing it in 2, 
 * then checking whether the element is in the 1st half of the 
 * structure, or the second.
 * 
 */

/**
 * Given an array of integers and a target number, find the target number within the array
 * if the number is in the array return its index, if not return -1;
 * @param {*} array  1 <= array.length <10^5
 * @param {*} target 0 < target < MAX_NUM
 */
